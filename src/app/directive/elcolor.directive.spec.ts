import { ElcolorDirective } from './elcolor.directive';

describe('ElcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElcolorDirective();
    expect(directive).toBeTruthy();
  });
});
